FILE *fptr;
    FILE *binaryFile;
    int i;
    char *line = (char*)malloc(80*sizeof(char));
    char *ptr;
    fptr = fopen("bar2.txt","r");
    fclose(fptr);
    ptr   = fileToArray(fptr);
    binaryFile = fopen("binaryFile.txt", "w");
    for (i = 0; i < simbCount; i++)
    {
        printTable(tab[i]);
    }
    //line = (char*)malloc(80*sizeof(char));
    codeToBinary(line,ptr,binaryFile);
    printf("\ngot here\n");
    fclose(binaryFile);